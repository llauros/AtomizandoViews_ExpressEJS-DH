<%- include('./partials/header') %>

    <main class="main">
      <div class="main_title">
        <h1><%= title %></h1>
      </div>
      <section>
        <ul>
            <% courses.forEach( course => {  %>
                <% console.log(course)  %> 
                <li><%= course %> </li>
            <% }); %>
        </ul>
      </section>
    </main>

<%- include('./partials/footer') %>